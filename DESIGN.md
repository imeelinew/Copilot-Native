# Design Rules

- Every interface must use SwiftUI, native system controls, or a proven implementation from a reference project
- Do not create custom drawn interfaces, controls, or button styles
- Assume that the agent does not have reliable visual design judgment by default
- Write interface copy when necessary
- Do not add secondary descriptions or gray supporting text without the user's explicit approval
- Interface copy must not contain periods, commas are allowed
